name: DeePAW
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pytorch>=2.0.0
  - pytorch-cuda=11.8
  - pip
  
  - pip:
    - e3nn>=0.5.0
    - ase>=3.22.0
    - pymatgen>=2023.0.0
    - scipy>=1.7.0
    - tqdm>=4.60.0
    - pykan>=0.2.0
    - accelerate>=0.20.0
    - matplotlib>=3.5.0

